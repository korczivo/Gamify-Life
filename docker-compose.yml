services:
  mongo:
    image: mongo:7
    container_name: empire-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - empire-mongo-data:/data/db

volumes:
  empire-mongo-data:
